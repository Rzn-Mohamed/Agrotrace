# 🏗️ Architecture DashboardSIG - Vue d'Ensemble

## 📐 Diagramme d'Architecture Globale

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          👨‍🌾 UTILISATEUR (Agriculteur)                    │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
                                 │ HTTP (Port 5173)
                                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         🌐 FRONTEND - React/Vite                         │
│ ┌──────────────────┐  ┌──────────────────┐  ┌────────────────────────┐│
│ │  MapComponent    │  │    Sidebar       │  │   ParcellePopup       ││
│ │   (Leaflet)      │  │  (Dashboard)     │  │    (Détails)          ││
│ │                  │  │                  │  │                        ││
│ │ • Carte 2D       │  │ • Stats globales │  │ • Info parcelle       ││
│ │ • Parcelles      │  │ • Alertes        │  │ • Alertes maladies    ││
│ │ • Layers         │  │ • Recommandations│  │ • Recommandations     ││
│ │ • Popup          │  │ • Export PDF     │  │ • Actions             ││
│ └────────┬─────────┘  └────────┬─────────┘  └──────────┬─────────────┘│
│          │                     │                         │              │
│          └─────────────────────┴─────────────────────────┘              │
│                                 │                                        │
│                         📡 API Service (Axios)                          │
│                         • getParcelles()                                │
│                         • getEtatHydrique()                             │
│                         • getAlertes()                                  │
│                         • getRecommandations()                          │
│                         • exportToPDF()                                 │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  │ REST API (Port 3001)
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      ⚙️  BACKEND - Node.js/Express                       │
│ ┌─────────────────────────────────────────────────────────────────────┐│
│ │                         📍 Routes (/api/*)                          ││
│ │  GET  /parcelles              GET  /etat-hydrique                   ││
│ │  GET  /parcelles/:id          GET  /alertes                         ││
│ │  GET  /recommandations        GET  /stats                           ││
│ │  POST /recommandations/:id/appliquer                                ││
│ └───────────────────────────────┬─────────────────────────────────────┘│
│                                 │                                       │
│ ┌─────────────────────────────────────────────────────────────────────┐│
│ │                   🎯 Controllers (Business Logic)                   ││
│ │  • getParcelles()         → SQL + ST_AsGeoJSON()                    ││
│ │  • getParcelleById()      → SQL + JOIN alertes/recommandations      ││
│ │  • getEtatHydrique()      → Mock IoT/Drone data                     ││
│ │  • getAlertes()           → SQL alertes_maladies                    ││
│ │  • getRecommandations()   → SQL recommandations_irrigation          ││
│ │  • getStats()             → SQL agrégations                         ││
│ └───────────────────────────────┬─────────────────────────────────────┘│
│                                 │                                       │
│ ┌─────────────────────────────────────────────────────────────────────┐│
│ │                      🔌 Database Config                             ││
│ │  • Connection Pool (pg)                                             ││
│ │  • Query helpers                                                    ││
│ └───────────────────────────────┬─────────────────────────────────────┘│
└─────────────────────────────────┼───────────────────────────────────────┘
                                  │
                                  │ PostgreSQL Protocol (Port 5432)
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                  🗄️  DATABASE - PostgreSQL 16 + PostGIS 3.4            │
│ ┌─────────────────────────────────────────────────────────────────────┐│
│ │                         📊 Tables                                   ││
│ │                                                                     ││
│ │  ┌────────────────────────────────────────────────────────────┐   ││
│ │  │ parcelles                                                   │   ││
│ │  │ • id, nom, culture, superficie_ha                          │   ││
│ │  │ • geometry GEOMETRY(Polygon, 4326) ← PostGIS               │   ││
│ │  │ • stress_hydrique (OK/MODERE/CRITIQUE)                     │   ││
│ │  │ • niveau_stress (0.0 - 1.0)                                │   ││
│ │  │ • besoin_eau_mm                                            │   ││
│ │  └──────────────────┬─────────────────────────────────────────┘   ││
│ │                     │                                              ││
│ │  ┌──────────────────┴─────────────────────────────────────────┐   ││
│ │  │ alertes_maladies                                            │   ││
│ │  │ • parcelle_id (FK)                                          │   ││
│ │  │ • type_maladie, severite, confiance                         │   ││
│ │  │ • zone_affectee GEOMETRY(Polygon, 4326)                     │   ││
│ │  └──────────────────┬─────────────────────────────────────────┘   ││
│ │                     │                                              ││
│ │  ┌──────────────────┴─────────────────────────────────────────┐   ││
│ │  │ recommandations_irrigation                                  │   ││
│ │  │ • parcelle_id (FK)                                          │   ││
│ │  │ • volume_mm, duree_minutes, heure_optimale                  │   ││
│ │  │ • priorite (BASSE/NORMALE/HAUTE/URGENTE)                    │   ││
│ │  └─────────────────────────────────────────────────────────────┘   ││
│ └─────────────────────────────────────────────────────────────────────┘│
│ ┌─────────────────────────────────────────────────────────────────────┐│
│ │                    🔍 Fonctions PostGIS                             ││
│ │  • ST_AsGeoJSON()        → Conversion GEOMETRY → JSON              ││
│ │  • ST_GeomFromGeoJSON()  → Conversion JSON → GEOMETRY              ││
│ │  • ST_Centroid()         → Centre d'un polygone                    ││
│ │  • ST_MakeEnvelope()     → Bounding box                            ││
│ └─────────────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────────────┘
```

## 🔄 Flux de Données Typique

### Scénario 1: Chargement de la Carte

```
1. User ouvre http://localhost:5173
        ↓
2. MapComponent.jsx monte
        ↓
3. useEffect() trigger
        ↓
4. api.getParcelles() → GET /api/parcelles
        ↓
5. Backend: parcelleController.getParcelles()
        ↓
6. SQL Query avec ST_AsGeoJSON():
   SELECT json_build_object(
     'type', 'FeatureCollection',
     'features', json_agg(
       json_build_object(
         'type', 'Feature',
         'geometry', ST_AsGeoJSON(geometry)::json,
         'properties', {...}
       )
     )
   ) FROM parcelles;
        ↓
7. Retour GeoJSON au Frontend
        ↓
8. setState(geojsonData)
        ↓
9. Leaflet render <GeoJSON> layers
        ↓
10. Carte affichée avec 4 parcelles colorées
```

### Scénario 2: Click sur Parcelle

```
1. User clique sur une parcelle (ID: 3)
        ↓
2. Event handler: onEachFeature.click
        ↓
3. api.getParcelleById(3) → GET /api/parcelles/3
        ↓
4. Backend: parcelleController.getParcelleById(3)
        ↓
5. SQL Query avec JOINs:
   SELECT p.*, 
     json_agg(alertes),
     json_agg(recommandations)
   FROM parcelles p
   LEFT JOIN alertes_maladies a ON p.id = a.parcelle_id
   LEFT JOIN recommandations_irrigation r ON p.id = r.parcelle_id
   WHERE p.id = 3;
        ↓
6. Retour objet parcelle complet avec alertes + recommandations
        ↓
7. setSelectedParcelle(data)
        ↓
8. <ParcellePopup> s'affiche en modal
        ↓
9. User voit:
   • Nom: "Parcelle Sud - C3"
   • Culture: "Tournesol"
   • Stress: "CRITIQUE" (85%)
   • Alerte: "Oïdium - Sévérité ELEVEE"
   • Recommandation: "Irriguer 60mm ce soir (URGENTE)"
```

### Scénario 3: Export PDF

```
1. User clique "Export PDF" dans Sidebar
        ↓
2. Sidebar.handleExport() déclenché
        ↓
3. exportToPDF(stats, alertes, recommandations, selectedParcelle)
        ↓
4. jsPDF génère document:
   • Header avec logo AgroTrace
   • Tableau statistiques globales
   • Détails parcelle sélectionnée (si applicable)
   • Tableau alertes maladies
   • Tableau recommandations irrigation
   • Footer avec pagination
        ↓
5. doc.save('AgroTrace_Rapport_2025-12-01.pdf')
        ↓
6. Navigateur télécharge le PDF
```

## 🐳 Architecture Docker

```
┌────────────────────────────────────────────────────────────────┐
│                    Docker Compose Network                       │
│                    (agrotrace-network)                          │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐│
│  │ Container: agrotrace-frontend                             ││
│  │ Image: nginx:alpine                                       ││
│  │ Port: 5173:80                                             ││
│  │ Files: /usr/share/nginx/html (React build)                ││
│  │ Health: wget http://localhost/                            ││
│  └───────────────────────────────────────────────────────────┘│
│                              │                                  │
│                              │ depends_on                       │
│                              ▼                                  │
│  ┌───────────────────────────────────────────────────────────┐│
│  │ Container: agrotrace-backend                              ││
│  │ Image: node:20-alpine                                     ││
│  │ Port: 3001:3001                                           ││
│  │ Env: DB_HOST=postgis                                      ││
│  │ Health: GET /api/health                                   ││
│  └───────────────────────────────────────────────────────────┘│
│                              │                                  │
│                              │ depends_on                       │
│                              ▼                                  │
│  ┌───────────────────────────────────────────────────────────┐│
│  │ Container: agrotrace-postgis                              ││
│  │ Image: postgis/postgis:16-3.4-alpine                      ││
│  │ Port: 5432:5432                                           ││
│  │ Volume: postgis_data:/var/lib/postgresql/data            ││
│  │ Init: /docker-entrypoint-initdb.d/init.sql               ││
│  │ Health: pg_isready -U postgres                           ││
│  └───────────────────────────────────────────────────────────┘│
└────────────────────────────────────────────────────────────────┘
```

## 🎨 Palette de Couleurs (Stress Hydrique)

```
┌──────────────┬─────────────────┬──────────┬─────────────────┐
│ État         │ Niveau          │ Couleur  │ Action          │
├──────────────┼─────────────────┼──────────┼─────────────────┤
│ OK           │ 0.0 - 0.3       │ 🟢 #27ae60│ Surveillance    │
│ MODERE       │ 0.3 - 0.6       │ 🟡 #f39c12│ Planifier       │
│ CRITIQUE     │ 0.6 - 1.0       │ 🔴 #e74c3c│ Urgent          │
└──────────────┴─────────────────┴──────────┴─────────────────┘
```

## 📊 Métriques & Performance

```
┌─────────────────────────────────────────────────────────────┐
│ Performance Cibles                                           │
├─────────────────────────────────────────────────────────────┤
│ Time to First Byte (TTFB)         < 200ms                   │
│ First Contentful Paint (FCP)      < 1.5s                    │
│ Largest Contentful Paint (LCP)    < 2.5s                    │
│ API Response Time                 < 100ms (95th percentile) │
│ Database Query Time               < 50ms  (average)         │
│ PDF Generation Time               < 3s                      │
└─────────────────────────────────────────────────────────────┘
```

## 🔒 Sécurité

```
┌─────────────────────────────────────────────────────────────┐
│ Couches de Sécurité                                          │
├─────────────────────────────────────────────────────────────┤
│ 1. Network Isolation     ✅ Docker network interne          │
│ 2. HTTPS (Production)    ⚠️  À configurer (Nginx + SSL)     │
│ 3. CORS                  ✅ Configuré (Frontend URL only)   │
│ 4. Helmet.js             ✅ Security headers                │
│ 5. SQL Injection         ✅ Parameterized queries (pg)      │
│ 6. XSS Protection        ✅ React auto-escape               │
│ 7. Rate Limiting         ⚠️  À ajouter en production        │
│ 8. Authentication        ❌ Non implémenté (roadmap)        │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 Scalabilité Future

```
Étape Actuelle:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Frontend │───▶│ Backend  │───▶│ PostGIS  │
└──────────┘    └──────────┘    └──────────┘
   (1 inst)       (1 inst)        (1 inst)

Étape 2 - Load Balancing:
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Frontend │───▶│  Nginx   │───▶│ PostGIS  │
│    x3    │    │   LB     │    │  (Master)│
└──────────┘    └────┬─────┘    └────┬─────┘
                     │               │
                ┌────▼──────┐       │
                │ Backend   │       │
                │   Pool    │       ▼
                │  (x3-5)   │  ┌──────────┐
                └───────────┘  │ PostGIS  │
                               │ (Replica)│
                               └──────────┘

Étape 3 - Microservices:
┌──────────┐    ┌────────────┐    ┌──────────┐
│ Frontend │───▶│ API Gateway│───▶│ Service  │
└──────────┘    └─────┬──────┘    │   IoT    │
                      │            └──────────┘
                      │            ┌──────────┐
                      ├───────────▶│ Service  │
                      │            │  Drone   │
                      │            └──────────┘
                      │            ┌──────────┐
                      ├───────────▶│ Vision   │
                      │            │  Plante  │
                      │            └──────────┘
                      │            ┌──────────┐
                      └───────────▶│   Reco   │
                                   │Irrigation│
                                   └──────────┘
```

---

**Architecture conçue pour: Simplicité ✓ Performance ✓ Évolutivité ✓**
